# ============================================================================
# GCP Deployment Environment Configuration Template
# ============================================================================
# Copy this file to .env and fill in your GCP-specific values for deployment
# Never commit .env to version control!

# Flask Application Settings (Production)
DEBUG=false                   # Disable debug mode in production
HOST=0.0.0.0                 # Bind to all interfaces for container networking
PORT=8080                    # Production server port

# Container Configuration
DOCKER_IMAGE_NAME=portfolio-app       # Docker image name for deployment
IMAGE_TAG=latest                      # Container tag (consider using version numbers)

# Google Cloud Platform Configuration
# Set these values for your specific GCP project and setup
GCP_PROJECT_ID=your-gcp-project-id              # Your GCP project ID
GCP_REGION=us-central1                          # Target GCP region
ARTIFACT_REGISTRY=your-artifact-registry        # Artifact Registry repository name

# Optional: Application Overrides
# APP_NAME="Portfolio Production"
# VERSION="1.0.0"

# Security Note:
# - These environment variables are used by scripts/deploy-gcp.sh
# - Never commit actual values to version control
# - Use CI/CD secrets for automated deployments
# - Ensure your .gitignore includes .env files

# Usage:
# 1. Copy this file: cp .env.deployment .env
# 2. Replace the placeholder values with your actual GCP configuration
# 3. Run deployment: ./scripts/deploy-gcp.sh