# Environment Configuration Template
# Copy this file to .env.development or .env.production and fill in your values
# Source: scripts/.env.example

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# Docker Image Settings
DOCKER_IMAGE_NAME=""              # Container image name (e.g., "portfolio-app")
IMAGE_TAG=""                      # Image tag (e.g., "v1.0.0", "dev-latest")

# Script Behavior
SCRIPT_VERBOSE=""                 # "true" for detailed output, "false" for quiet
SCRIPT_AUTO_START=""              # "true" to auto-start services, "false" for manual

# Development Settings (development only)
DEV_PORT=""                       # Local development port (e.g., "8080")

# =============================================================================
# GOOGLE CLOUD PLATFORM CONFIGURATION
# =============================================================================
# Set these values for GCP deployment

# Core GCP Settings
GCP_PROJECT_ID=""                 # Your GCP project ID
GCP_REGION=""                     # Deployment region (e.g., "us-central1")
ARTIFACT_REGISTRY=""              # Container registry name

# Cloud Run Settings
CLOUD_RUN_SERVICE=""              # Cloud Run service name (optional)
CLOUD_RUN_PORT=""                 # Container port (optional, defaults to 8080)

# Security & Access
SERVICE_ACCOUNT=""                # Service account email (optional)
ALLOW_UNAUTHENTICATED=""          # "true" for public access, "false" for authenticated

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# These override pyproject.toml defaults

# Application Settings  
APP_NAME=""                       # Application display name
DEBUG=""                          # "true" for debug mode, "false" for production
HOST=""                           # Bind host (e.g., "0.0.0.0")
PORT=""                           # Application port

# Database & External Services
DATABASE_URL=""                   # Database connection string (if applicable)
REDIS_URL=""                      # Redis connection string (if applicable)

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env.development    # For development
#    cp .env.example .env.production     # For production
#
# 2. Fill in the appropriate values for your environment
#
# 3. The actual .env files are ignored by git for security
#
# 4. Required for GCP deployment:
#    - GCP_PROJECT_ID
#    - GCP_REGION  
#    - ARTIFACT_REGISTRY
#
# 5. Optional but recommended:
#    - DOCKER_IMAGE_NAME
#    - IMAGE_TAG
#    - SCRIPT_VERBOSE
